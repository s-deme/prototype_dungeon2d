# MVP実装・検証状況

2026-09-07。Phase 1〜12を実装しました。Unity 6000.3.22f1でインポート・コンパイル・テストを実行し、Windowsビルドと実行画面を検証しました。

製品化パス1〜3も完了。最新検証は.NET/Unity EditMode各23件成功、Unity PlayMode5件成功・対話式撮影1件スキップ。
Windows配布ZIPは `Builds/LanternDepths-Windows.zip`。最新の受入確認は末尾の完了監査を参照。

| Phase | 実装証拠 | 現在の検証 |
|---|---|---|
| 1 グリッド | GridPosition / TileData / DungeonMap | .NETおよびUnity EditModeのGridTests合格 |
| 2 移動 | MovementRules / FloorState / CharacterState | 8方向・壁・境界・占有・角抜けテスト合格 |
| 3 ターン | Actions / ActionResolver / TurnProcessor | 成功時のみ進行・全敵1回・死亡後拒否テスト合格 |
| 4 敵AI | BasicEnemyBrain | 隣接攻撃・遮蔽・BFS迂回・到達不能テスト合格 |
| 5 戦闘・成長 | CombatResolver / PlayerProgression | 最低ダメージ・死亡・EXP二重付与防止・複数成長テスト合格 |
| 6 自動生成 | RoomCorridorGenerator / FloorFactory | 100 Seedの連結性・再現性・配置重複テスト合格 |
| 7 階移動 | RunController | 状態維持・敵行動スキップ・生成失敗時保全テスト合格 |
| 8 アイテム | Items / Content / Data | 定義・床配置・回復テスト合格。Asset実インポート・Windowsで回復品消費を確認 |
| 9 所持品 | Inventory / ItemActionResolver | 容量・取得・消費・失敗時保全テスト合格。空・満杯の所持品画面を実表示して確認 |
| 10 装備 | Equipment / StatCalculator | 武器防具・交換・解除・破棄・階移動維持テスト合格 |
| 11 UI・ログ | Presentation / Game.uss / Main.unity | Unity PlayMode・入力割当テスト合格。3解像度で画面確認。演出中入力拒否・ログ・所持品モーダルを検証 |
| 12 ゲームオーバー | RunController / HudPresenter | Domainの死亡後拒否・探索初期化合格。Windowsで死亡画面と再挑戦を確認 |

## MVP時点の検証（履歴。最新結果は末尾）

- `dotnet test Tools/Domain.Tests.csproj`: 16テスト成功、失敗0。20 Seed×3階の取得・装備・階移動統合テストを含む。
- Unity 6000.3.22f1 EditMode: 16テスト成功、失敗0 (`TestResults/EditMode.xml`)。
- Unity PlayMode: 3成功、失敗0、スキップ1 (`TestResults/PlayMode.xml`)。スキップはEditor対話式画面キャプチャテストで、Windows開発ビルドからの7枚の実画面取得・目視確認で代替しました。
- 入力テスト: キーボード・テンキー・Xbox形式の入力値を注入し、8方向、操作割当、押しっぱなし・スティックを戻す途中の余分な行動防止を検証。
- 統合テスト: 20 Seed×3階で取得・回復・装備・経路上の戦闘・階移動を検証。ここでは敵を待機に固定し、通常AIは別テストで検証。
- `dotnet build Tools/Unity.Compile.csproj`: エラー0。Inspector割当フィールドのCS0649警告あり。
- UI色コントラスト: 通常文字8組すべて4.5:1以上。
- サンドボックスアカウントではライセンス参照に失敗したが、通常ユーザー権限では認証・起動に成功。
- Windows開発ビルド: `TestResults/PlayerSmoke-final/result.txt` にPASS。起動、所持品、装備、回復、演出中入力拒否、リサイズ、死亡、再挑戦を検証。
- 通常Windowsビルド: `TestResults/build-windows.log` の `Build Finished, Result: Success.`。実行ファイルの起動も確認。

## 画面確認と再実行

1280×900、1280×720、1920×1080の探索画面、空/満杯の所持品、死亡、再挑戦を確認しました。
代表画像: [探索](screenshots/exploration.png)、[所持品](screenshots/inventory.png)、[死亡](screenshots/game-over.png)。画像は検証用Development Buildの実画面です。

実画面で見つかった黒画面、操作説明とボタンの重なり、所持品下部の重なり、マップ下端の切れを修正しました。結果ファイルがPASSであることだけを画面正常性の根拠にはしていません。

再実行は通常ユーザーから `Tools/Verify.ps1 -Unity -Build`。
開発用実行ファイル内検証は、Unityの`LanternDepths.Editor.BuildGame.WindowsSmoke`を`-executeMethod`でビルドし、`Builds/Smoke/LanternDepths.exe --smoke-test --smoke-output=<新規出力フォルダー>`で実行します。通常ビルドにはこの自動操作コードは含まれません。

## 制限

- ゲームパッドは入力値注入で検証済みですが、物理コントローラーの接続検証は未実施です。他形式の機種では割当調整が必要な場合があります。
- UIは英語、グリフによる仮アートです。音楽・音声はありません。
- Seed再現は同一ランタイム・同一コード・同一設定・同一入力列が対象です。

## MVPで意図的に未導入

空腹、状態異常、FOV、Android、音楽、特殊アイテム、汎用EventBus、DIコンテナ。
セーブはMVP後の製品化パス2で追加。
純粋なC#状態と明示的なターン境界を将来の機能追加位置とします。

## 製品化パス（2026-09-07）

現状監査: 基本の探索・戦闘・取得・装備・階移動・死亡/再挑戦は実装済み。
製品としての主要な不足を次の順に扱う。MVP完了は製品化完了を意味しない。

| 優先度 | 不足と受入条件 | 状態 |
|---|---|---|
| 必須 | 探索の目的とクリア: 目標が画面に表示され、最終階の階段でクリア、結果表示、操作停止、再挑戦 | 実装・検証済み。標準5階、GameRulesで調整可 |
| 必須 | 中断・再開: 終了後に探索を復元し、破損データで黙って進行を失わない | 実装・検証済み。自動保存・バックアップ・破損保全・復元、別プロセスでの再開成功 |
| 必須 | メニューと設定: 終了操作、操作説明、設定の保持、誤操作による探索破棄を防ぐ | 実装・検証済み。Esc/Startのメニュー、演出設定保存、新規探索/復元の確認 |
| 必須 | 配布情報: 現在の動作と制限、起動方法、利用素材の権利情報を整える | 完了。案内・素材一覧・Unity通知原文・ビルド情報を自動同梱。ZIP展開後の全147ファイル一致 |
| 後続 | FOV、空腹、状態異常、特殊アイテム、音楽、別プラットフォーム | 今回は探索の完結と継続性を優先。必要性未確定 |

パス1: 勝利を死亡と区別し、共通ターン処理と入力の両方で終了後の行動を拒否。
最終階では次階を生成せず、1ターンだけ進め、敵の反撃なしで宝火を回収する。
通常階から最終階へ移動→クリア→全コマンド拒否→新規探索の回帰テストを追加。

検証: .NETとUnity EditModeは各17件成功。PlayModeは3件成功・画面キャプチャ1件スキップ。
Windows開発ビルドの `TestResults/ProductPass1Final/result.txt` はPASS。
1280×720の探索画面と1920×1080の[クリア画面](screenshots/victory.png)を目視確認し、
目標文追加によるサイドバー下端のはみ出しを余白調整で修正・再撮影した。
開発ビルドのクリア検証は階段へ直接配置して結果UIを確認するもので、通常経路はDomainテストで確認。
検証スクリプトのUnity終了待ちも修正（常駐する補助プロセスを待たず、Editor本体の終了を待つ）。

パス2: 毎ターンの自動保存、起動時の再開、保存して終了、演出設定の保存を追加。
保存形式はバージョン・ルール指紋・チェックサムを持つ現在階のスナップショット。
地形・生存敵・HP・EXP・所持品・装備・結果・乱数状態を保存し、死亡/クリア後も結果から再開する。
`RunRandom`で乱数状態を明示的に保持するため、セーブ導入前の同じSeedとは配置が変わる。
旧バージョン/異なる設定の保存は読み込み時に拒否し、無断上書きしない。ログ履歴は再開時にリセット。
一時ファイルを書き出してから置き換え、前回の異なる保存を`.bak`へ残す。同一データの再保存でバックアップを潰さない。
破損時は復元または新規探索の確認まで操作停止。書き込み失敗時は現在のメモリーと前回ファイルを保全し、再試行を案内する。
Unityの破損ファイルテストで見つけたInvalidDataExceptionの捕捉漏れも修正した。

パス2最終検証: .NET/Unity EditMode各21件成功、Unity PlayMode4件成功・対話式撮影1件スキップ。
通常Windowsビルドは `TestResults/build.log` にSuccess。
`TestResults/ProductPass2Final/result.txt` は保存失敗/再試行・破損拒否・バックアップ復元・設定保持を含めPASS。
保存して終了した後、別プロセスで起動した `resume-result.txt` もPASS。
[メニュー](screenshots/menu.png)、新規探索確認、保存失敗、[読み込み失敗](screenshots/save-recovery.png)、1280×720の探索、再起動後の画面を目視確認。
低解像度で重なっていたマップ見出しを、見出しの縮小防止とマップ高さ調整で修正した。
テストの保存先は専用フォルダーで、通常プレイのセーブや演出設定は変更していない。

パス3: プレイヤー用案内、素材/ランタイム一覧、Unity 6000.3.22f1の通知原文、バージョン情報をビルドへ同梱。
`docs/PLAYER_GUIDE.txt`→`READ_ME.txt`、`docs/CREDITS.txt`、Editorの`Resources/legal.txt`をビルド処理でコピーする。
同梱案内と通知は原本とのハッシュ一致を確認。ゲーム固有部分への新しいライセンス付与はしていない。
再監査で見つかった多重起動時のセーブ競合は、保存フォルダー単位のOSファイルロックで防止。
ロック取得失敗時は保存を触らず、別のゲームを閉じる案内とQuitを表示する。終了/シーン再読込で解放する。
検証スクリプトも古いXMLを削除し、新規テスト結果と成功件数を確認するようにした。

通常AI・製品と同じ能力/アイテム設定による通し検証: Seed 0〜4の全5件でクリア。
取得・装備・回復・戦闘・通常経路での5階移動に加え、25操作ごとの保存/復元を通した。
ターン数は473/492/515/492/724。これは到達可能性の検証で、プレイヤー全体の勝率を示すものではない。

## 完了監査（2026-09-07）

| 受入項目 | 現在の証拠 |
|---|---|
| 探索・所持品・戦闘・成長・5階クリア・再挑戦 | EditMode 23件成功。通常AIの5 Seed完走、Windows開発ビルドの結果画面/再挑戦成功 |
| 保存内容と再開後の乱数・次階一致 | SaveTestsのスナップショット比較、将来生成比較。PlayMode再読込、別プロセス再開成功 |
| 保存失敗・破損・互換性不一致・バックアップ復元 | SaveTests、SceneTests、`TestResults/ProductFinalScreens/result.txt`がPASS |
| メニュー中の操作停止・新規探索確認取消・設定保持 | InputTests/SceneTests、Windowsのメニュー/設定/確認取消操作がPASS |
| 多重起動で保存を上書きしない・ロックを解放する | ファイル共有テスト、PlayModeの拒否画面テスト、Windows開発ビルドの重複Bootstrap拒否と画面確認、終了後の別プロセス再開 |
| 画面の可読性と操作 | 3解像度、探索/所持品/結果/メニュー/保存エラーの実画面確認。多重起動エラーも描画完了後に確認 |
| 配布に必要なファイルと情報 | `TestResults/build.log`がSuccess。通常版1.0・非Development。ZIP展開後147ファイルすべて元ビルドと一致（`package-verification.txt`） |

最終実行記録は `TestResults/ProductFinalScreens/result.txt` と `resume-result.txt`。
ZIPのSHA256は隣接する`LanternDepths-Windows.zip.sha256`に記録。
物理ゲームパッドは未検証、UIは英語・グリフ表現、音声なしという制限はプレイヤー案内にも明記。
FOV・空腹・状態異常・別プラットフォームは既存製品方向を変える追加設計のため、今回の必須機能には含めない。

## 機能拡張 1.1（2026-09-07）

不足機能監査の高・中優先度を実装。先行する1.0の完了監査は当時の記録として残す。

- 戦闘：近接、射程4で後退する射手、予告して強打する最終階の守護者。守護者生存中は回収不可。
- 階別変化：1階は近接中心、2階は射手中心。後半の追加道具が階別に変わり、5階は守護者戦。
- 道具：退避ルーン（通行可能な4歩以内の安全な位置）、爆薬瓶（遮られていない2マスの敵に8ダメージ）。対象なしでは消費しない。
- 装備：攻撃+6/防御-2の大剣、防御+5/攻撃-2の板金鎧。種類・名称順の一覧と、下限0を考慮した能力比較。
- 敵情報：クリック/Tab/右スティック押し込みでHP、攻撃、防御、役割と予告状態を表示。
- 設定：日本語/English、文字100/110/120%、効果音0〜100%、全画面/ウィンドウ、キー変更と初期化。キー競合は交換、矢印・テンキー・ゲームパッドは固定。
- 導入：初回の遊び方、後から再表示できるヘルプ。補助画面中は行動停止。
- 効果音：攻撃/被弾、取得、使用、階移動、成長、クリア、強打予告。波形を実行時生成し、外部音源は追加しない。
- 記録：直近1000冒険の結果と集計、現在の冒険の直近200行。復元先より新しい行動履歴を読み込まない。
- セーブ：形式2。形式1の実ファイルを移行し、現在階の状態と旧敵行動を保持。次階から新内容。未知の形式や不一致ルールは拒否。

検証：.NET 27件、Unity EditMode 27件、PlayMode 7件成功（対話式撮影1件はスキップし、Windowsプレイヤーで撮影）。
通常AI・製品と同じ7道具によるSeed 0〜4の通し検証は3勝2敗。勝利ターン573/596/808。到達可能性の確認で、一般プレイヤーの勝率ではない。
実行記録は `TestResults/FeatureFinal/result.txt` と `resume-result.txt`。設定保持、初回案内、日本語/英語切替、文字拡大、守護者ゲート、履歴、保存エラーと復元、終了後の別プロセス再開を確認。
1280×720、1280×900、1920×1080の実画面を確認。日本語はWindowsの游ゴシック等を使用しフォントを再配布しない。
Direct3D 12で非表示起動時のキャプチャが黒くなったため、確認できたDirect3D 11をWindowsビルドの標準に指定。
通常ビルドは1.1・非Development・保存形式2。物理ゲームパッドおよびスピーカーからの聴取は未検証。
FOV、空腹、状態異常、BGM、オンライン、他プラットフォームは今回の対象外。
